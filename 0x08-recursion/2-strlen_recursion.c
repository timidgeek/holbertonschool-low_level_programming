#include "main.h"
#include "_putchar.c"
#include "_strlen.c"

/**
* _strlen_recursion - returns the length of a string
* @s: string
* Return: length of string
*/
int _strlen_recursion(char *s)
{
	char len = _strlen(s);

	return (len);
}
