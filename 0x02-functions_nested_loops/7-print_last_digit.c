#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 * @lastDig: variable
 *
 * Return: 0.
*/
int print_last_digit(int lastDig)
{
	if (lastDig % 10)
	{
		_putchar(lastDig % 10);
	}
return (0);
}
