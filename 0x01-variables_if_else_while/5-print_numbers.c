#include <stdio.h>
/**
 * main - Printing all single digit numbers of base ten
 *
 * Return: Always 0 (success)
*/
int main(void)
{
int num;

for (num = 0; num < 10; num++)
{	printf("%d\n", num);
}
return (0);
}
